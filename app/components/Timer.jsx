var React = require("react");

var Timer = React.createClass({
  render: function() {
    return (
      <div>
        <h1 className="title">Timer</h1>
      </div>
    );
  }
});

module.exports = Timer;
